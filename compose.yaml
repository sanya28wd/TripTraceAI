services:
  web:
    image: nginx:1.27-alpine
    ports:
      - "8080:80"
    volumes:
      - "./web:/usr/share/nginx/html:ro"

  api:
    build:
      context: ./api
    ports:
      - "8000:8000"
